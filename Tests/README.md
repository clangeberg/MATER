# Test programs

These Swift entry points exercise MATER's model, document behavior, editing invariants, export, randomized operations, Rfam compatibility, performance, and offscreen GUI rendering.

The shell commands that compile and run them are documented in [the scripts guide](../scripts/README.md).
